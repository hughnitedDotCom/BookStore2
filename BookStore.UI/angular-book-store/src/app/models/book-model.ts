export class Book{
    constructor(bookId: number, name: string, text: string, purchasePrice: number)
    {
        this.bookId = bookId;
        this.name = name;
        this.text = text;
        this.purchasePrice = purchasePrice
    }
        bookId: number;
        name: string;
        text: string
        purchasePrice: number
}